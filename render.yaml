# render.yaml — Docker deploy (needed for Playwright)
services:
  - type: web
    name: troopod-ai-personalizer
    runtime: docker
    plan: free
    envVars:
      - key: GROQ_API_KEY
        sync: false